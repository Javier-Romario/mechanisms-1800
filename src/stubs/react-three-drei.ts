/**
 * Build-time stub for the optional `@react-three/drei` peer dependency.
 * See `react-three-fiber.ts` — never executed, only used to resolve the
 * NEONDECK barrel imports so unused 3D components can be tree-shaken.
 */
export const OrbitControls = () => null;
export const Stars = () => null;
export default { OrbitControls, Stars };
